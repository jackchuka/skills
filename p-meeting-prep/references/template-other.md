# Template: Other / Generic Brief

For meetings classified as `other`, no source fetches were run. The brief is minimal:

```markdown
### HH:MM–HH:MM <Meeting title>  `[other]`
Attendees: <names>
Meet: <link or "—">

<event description verbatim, or "_(no description)_" if empty>
```

No body sections. No `Related today:` line — `other` meetings do not participate in clusters.
